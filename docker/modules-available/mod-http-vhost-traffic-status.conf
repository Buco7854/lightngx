# nginx-module-vts (no Debian package — compiled into the full image).
# The full image's entrypoint links this into modules-enabled by default,
# so the module is loaded; add the VTS zone + dashboard to your own config
# to actually use it.
load_module /usr/lib/nginx/modules/ngx_http_vhost_traffic_status_module.so;
